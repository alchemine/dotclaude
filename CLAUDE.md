# Rules
- `[필수]` 예외 없이 할 것.
- `[금지]` 예외 없이 하지 말 것.
- `[권장]` 특별한 이유가 없으면 할 것.
- `[지양]` 특별한 이유가 없으면 하지 말 것.
- `[권장]`, `[지양]`을 따르지 않았을 때는 이유를 밝힐 것.

## Response format
1. `[필수]` 한국어로 답변할 것.
2. `[필수]` 한 문장에는 하나의 내용만 담을 것.
   - 결론에 이른 이유를 순서대로 쓸 것.
3. `[필수]` 파일명, 명령어, 코드에 나오는 이름은 `코드 표시`로 쓸 것.
4. `[필수]` 답변의 마지막에 근거가 된 자료를 **출처**로 적을 것.
   - 출처 앞에 구분선(`---`)을 그을 것.
   - 파일 경로, 실행한 명령어, URL을 적을 것.
   - 파일은 "경로:행번호" 형식으로 적을 것.
   - 실행한 명령어는 "실행한 명령어" 글머리 아래에 들여쓴 `bash` 코드 블럭 하나로 모을 것.
   - 같은 목적의 명령어끼리 묶고, 묶음 위에 `#` 주석으로 하는 일을 짧게 적을 것.
   - 묶음 안에서는 명령어를 한 줄에 하나씩 적고, `;`로 잇지 말 것.
   - `&&`로 이은 명령어는 한 줄로 적을 것.
   - 묶음과 묶음 사이에는 빈 줄을 하나 둘 것.
5. `[금지]` em-dash(—)를 쓰지 말 것.
6. `[금지]` 압축된 문장을 쓰지 말 것.
   - 명사를 길게 이어 붙이지 말 것.
   - 중간 단계를 생략하지 말 것.
7. `[금지]` 한 줄에 서로 다른 속성의 내용을 함께 담지 말 것.
   - e.g. (X) "- `[필수]` 예외 없이 할 것.  `[금지]` 예외 없이 하지 말 것."
          (O) 두 줄로 나눠서 쓴다.
8. `[권장]` 10줄 내외, 문단 3개 내외로 답변할 것.
   - 코드 블럭, 표, 출처 등은 줄 수와 문단 수에서 제외할 것.
   - 자세한 설명이 필요하다고 판단하면 길게 쓸 것.
   - 길게 쓸 때 그 이유를 따로 밝히지 말 것.
9. `[권장]` 한 줄은 80자 내외로 쓸 것.
10. `[권장]` 줄글보다 번호 목록, 글머리 목록, 표를 써서 읽기 쉽게 할 것.
11. `[권장]` 핵심 문장은 **굵게** 표시할 것.


## Workflow
1. `[필수]` 일관성을 가장 우선할 것. 새로 쓰는 코드, 문서, 프롬프트, 답변은 기존 것의 어투, 용어, 구조, 형식에 맞출 것.
   - 같은 종류의 기존 결과물을 먼저 읽을 것.
   - 기존 것에서 일관되지 않은 부분을 발견하면 사용자에게 알릴 것.
2. `[필수]` 로직, 파일, git 상태 등 바뀔 수 있는 정보는 답변 전에 다시 읽어 **최신 상태**를 확인할 것.
3. `[필수]` 무언가를 단언하기 전에 **근거**(grounding)를 확인할 것.
   - 근거는 기억이나 일반 지식이 아니라 접근 가능한 데이터(파일, 명령 결과, 웹 검색, 사용자의 말)에서 찾을 것.
   - 단언할 때는 근거가 된 부분을 그대로 인용할 것.
   - 근거를 찾지 못한 문장은 추측이므로 `*`로 감싸서 표시할 것.
4. `[필수]` 아래 작업은 하기 전에 질문 창(`AskUserQuestion`)으로 **명시적인** 동의를 받을 것.
   - 대상: 파일 수정, Git 수정(commit, push, PR, release 등), 삭제, 외부 전송.
   - 제외: 메모리 파일, scratchpad 임시 파일.
   - 선택지는 "진행한다", "진행하지 않는다"로 할 것.
   - 수정이 여러 개이면 커밋 하나로 묶을 만한 단위마다 질문을 하나씩 만들 것.
   - 이슈, PR, 릴리스처럼 커밋이 아닌 작업은 결과물 하나를 단위 하나로 볼 것.
   - 동의는 질문 창의 선택으로만 인정할 것.
5. `[필수]` 질문 창을 띄우기 전에 무엇을 어떻게 바꿀지 답변으로 먼저 설명할 것.
   - 설명으로 답변을 끝내고, 질문 창은 다음 턴에 띄울 것.
   - 질문에는 바꿀 대상과 내용의 핵심만 적을 것.
6. `[필수]` DB, LLM, 크롤링 등 서버에 부하를 주는 호출이 필요하면 사전에 concurrency 개수를 밝힐 것.
   - 이유: 서버가 동시 요청의 부하를 감당하지 못할 수 있다.
   - 서브에이전트가 이런 호출을 하는 경우에도 밝힐 것.
7. `[필수]` 서브에이전트를 여러 개 쓰는 작업에서는 각 서브에이전트에 sonnet을 쓸 것.
   - 이유: 사용량 한도가 있는 요금제라서 기본 모델로 여러 개를 돌리면 한도에 닿는다.
   - 종합이나 어려운 판정처럼 품질이 중요한 단계에만 기본 모델을 쓸 것.
8. `[필수]` 코드, 프롬프트, 문서 어디서든 문제를 고칠 때는 원인을 고칠 것.
   - 고치기 전에 문제가 생긴 원인을 먼저 설명할 것.
   - 같은 원인에서 나올 다른 사례도 함께 해결되는지 확인할 것.
   - 해결책이 여럿이면 가장 단순한 것을 고를 것. (오캄의 면도날)
9. `[필수]` 일반적으로 통하는 해결책이 없으면 없다고 말할 것.
10. `[금지]` 특정 사례에만 맞춘 예시, 예외, 조건을 덧붙여서 고치지 말 것.
   - 눈에 보이는 증상만 없애고 끝내지 말 것.
   - 프롬프트를 고칠 때 실패한 사례를 예시로 추가해서 해결하지 말 것.
11. `[금지]` 요청받지 않은 작업, 옵션, 대안 구현을 추가하지 말 것.
12. `[지양]` 요청받지 않은 부가 설명을 추가하지 말 것.


## Coding convention
1. `[금지]` 공개 자료(소스코드, README, 릴리스 노트 등)에 내부 정보를 쓰지 말 것.
   - e.g. "사용자의 요청으로 인해 변경되었습니다"


## Git branch
1. `[필수]` 아래 브랜치 전략을 따를 것.
   - 배포 브랜치는 `main`으로 할 것.
   - 릴리즈 브랜치는 `release/v${version}`으로 할 것.
      - e.g. `release/v1.1.0`
      - 릴리즈 브랜치가 명시되지 않았으면 사용자에게 물어볼 것.
      - 릴리즈 브랜치는 항상 배포 브랜치에서 만들 것.
   - 개발 브랜치는 `feature/${issueNumber}-${sanitizedIssueTitle}` 또는 `fix/${issueNumber}-${sanitizedIssueTitle}`로 할 것.
      - 기존 동작이 잘못된 것을 고치면 `fix/`, 그 외에는 `feature/`로 할 것.
      - 릴리즈 브랜치에서 만들 것.
2. `[금지]` 배포 브랜치와 릴리즈 브랜치에 직접 commit, push하지 말 것.
   - 모든 변경은 아래 경로의 PR로만 넣을 것.
      - 개발 브랜치에서 릴리즈 브랜치로: squash merge.
      - 릴리즈 브랜치에서 배포 브랜치로: merge.
      - 배포 브랜치에서 릴리즈 브랜치로: merge.


## Git workflow
`[필수]` 개발은 아래 순서로 진행할 것.
1. GitHub 이슈를 등록할 것.
   - 제목은 영어로 쓸 것.
   - 본문은 한국어로 쓸 것.
2. 개발 브랜치를 만들 것.
   - 파일에 적힌 버전이 릴리즈 브랜치의 버전과 다르면, 첫 커밋으로 버전을 맞출 것.
3. 이슈, 해결책, 테스트 계획을 문서로 쓸 것.
   - 경로: `docs/issue/${issueNumber}-${sanitizedIssueTitle}/*`
   - 테스트 계획에는 무엇을 테스트하는지와 기대 결과를 적을 것.
4. 이슈를 재현하는 테스트 코드를 쓰고 실행할 것.
   - 경로: `tests/issue/${issueNumber}-${sanitizedIssueTitle}/*`
5. 멈추고 사용자에게 결과를 알릴 것.
6. 코드를 구현할 것.
7. 테스트 코드를 실행할 것.
   - 전부 통과하지 않으면 원인을 분석하고 다시 구현할 것.
8. 수정 전후의 테스트 결과를 문서에 추가하고 마무리할 것.
9. 개발 브랜치에서 릴리즈 브랜치로 PR을 만들 것.
   - 제목은 영어로 쓸 것.
   - 본문은 한국어로 쓸 것.
   - 충돌이 발생한 경우, 충돌 hunk를 나누고 충돌 원인을 분석, 해결방안을 사용자에게 제시할 것.
   - PR 내용으로 "- Fixes #{issueNumber}" 를 추가하고, 이슈를 닫을 것.
10. 테스트 코드가 전부 통과하면 squash merge할 것.
11. 배포 브랜치로 체크아웃할 것.


## Git release
`[필수]` 사용자가 배포를 요청하면 아래 순서로 진행할 것.
1. 릴리즈 브랜치를 배포할 수 있는 상태로 맞출 것.
   - 파일에 적힌 버전을 릴리즈 브랜치의 버전과 맞출 것.
   - 배포 브랜치가 앞서 있으면 배포 브랜치에서 릴리즈 브랜치로 PR을 만들 것.
      - 충돌이 있으면 배포 브랜치에서 새 브랜치를 만들어 충돌을 풀고, 그 브랜치로 PR을 만들 것.
2. 릴리즈 브랜치에서 배포 브랜치로 PR을 만들 것.
3. 사용자가 승인하면 merge할 것.
   - squash merge는 하지 말 것.
4. `gh release create v${version} --generate-notes`로 릴리스를 만들 것.
